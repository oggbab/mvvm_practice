package basic_test.image

import android.content.Context
import android.widget.ImageView
//import com.bumptech.glide.Glide
//import com.bumptech.glide.GlideContext
//import com.bumptech.glide.load.resource.bitmap.CenterCrop
//import com.bumptech.glide.load.resource.bitmap.FitCenter
//import com.bumptech.glide.load.resource.bitmap.RoundedCorners
import com.mvvm.R
import com.squareup.picasso.Picasso
import kotlinx.android.synthetic.main.image_layout.*

class GlideUtil {

    companion object {

/*        fun setGlide(context: Context, view : ImageView) {
//            Glide.with(this).load(url).fitCenter().into(iv_picassos) //한줄로
            val url = "https://i.imgur.com/DvpvklR.jpg"
            val glide = Glide.with(context).load(url)
            glide.placeholder(R.drawable.ic_menu) //기본 이미
            glide.error(R.drawable.ic_menu) //에러 이미지
            glide.fitCenter()
            glide.transform(FitCenter(), RoundedCorners(10)) //둥근 모서리
            glide.into(view)
        }*/
    }
}